import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reactudemy.toolbox',
  appName: 'All In One ToolBox',
  webDir: 'dist'
};

export default config;
